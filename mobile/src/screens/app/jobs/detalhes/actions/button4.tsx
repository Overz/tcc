import React from 'react';

/**
 * sou o contratante? sim
 * tem alguem trabalhando? sim
 * esta na validade? nao
 * posso renovar? sim
 * data de entrega valida? `não` -> VAZIO
 *
 * botao1: vazio
 * botao2: vazio
 * botao3: vazio
 */
export const Button4: React.FC<{ show: boolean }> = () => null;
